# Markdown Math

**Notice:** This extension is bundled with BatikCode. It can be disabled but not uninstalled.

Adds math rendering using [KaTeX](https://katex.org) to BatikCode's built-in markdown preview and markdown cells in notebooks.
